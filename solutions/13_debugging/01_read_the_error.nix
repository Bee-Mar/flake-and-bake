{
  q1 = "a";  # x and y each demand the other's value — no base case
  q2 = "b";  # ${...} needs a string; 80 is an int (a is fine: toString)
  q3 = "c";  # no `...`, so the typo'd attr is rejected (b's ... accepts it)
}
